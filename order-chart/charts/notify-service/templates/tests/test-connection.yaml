apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "notify-service.fullname" . }}-test-connection"
  labels:
    {{- include "notify-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "notify-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
